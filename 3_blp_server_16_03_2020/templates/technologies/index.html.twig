{% extends 'base.html.twig' %}

{% block body %}
    <div class="page-header">
        <div class="row align-items-center">
            <div class="col-auto">
                <h2 class="page-title">
                    Технологии и Новости
                </h2>
            </div>
            <div class="col-auto">
                <div class="card-options">
                    <a href="{{ path('technologies_new') }}" class="btn btn-primary">+ Добавить</a>
                </div>
            </div>
        </div>
    </div>
    <div class="row row-deck">
        {% for technology in technologies %}
            <div class="col-sm-6 col-xl-4">
                <div class="card d-flex flex-column">
                    <a href="{{ path('technologies_edit', {'id': technology.id}) }}">
                        <img class="card-img-top" src="{{ vich_uploader_asset(technology, 'imageFile') }}" alt="">
                    </a>
                    <div class="card-body d-flex flex-column">
                        <h3 class="card-title"><a href="{{ path('technologies_edit', {'id': technology.id}) }}">{{ technology.title }}</a></h3>
                        <div class="text-muted">{{ technology.anonce }}</div>
                        <div class="d-flex align-items-center pt-5 mt-auto">
                            Опубликовано: {{ technology.isActive ? '<span class="badge bg-green mr-2">да</span>' : '<span class="badge bg-red mr-2">НЕТ</span>' }}

                            <a href="{{ path('technologies_edit', {'id': technology.id}) }}" class="btn btn-secondary mr-2">Редактировать</a>
                            {{ include('technologies/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            Пока нет данных
        {% endfor %}
    </div>
{% endblock %}
